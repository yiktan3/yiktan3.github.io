# Yiktan Portfolio

Static portfolio website deployed with GitHub Pages.

## Edit website text

Open `content.js`, change the text, and save the file.

## Update the live website

After editing, commit and push the changes to the `main` branch. The GitHub Pages workflow in `.github/workflows/pages.yml` automatically republishes the same public URL.

